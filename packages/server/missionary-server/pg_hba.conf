# Local development: trust all connections (no password required)
# TYPE  DATABASE  USER  ADDRESS       METHOD
local   all       all                 trust
host    all       all   0.0.0.0/0     trust
host    all       all   ::0/0         trust
